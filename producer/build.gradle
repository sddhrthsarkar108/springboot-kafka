dependencies {
    implementation project(':common')
}

tasks.register('buildDockerImage', Exec) {
    commandLine 'docker', 'build', '-t', 'mod1', '.'
    workingDir project.projectDir
}

//tasks.build.dependsOn tasks.buildDockerImage